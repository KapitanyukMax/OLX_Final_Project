import {Box} from '@mui/material';
import { styled } from '@mui/material/styles';


export const LabelContainer = styled(Box)({
    display: 'flex',
    alignItems: 'center',
    gap: '8px',
});

export const LabelTextBlock = styled(Box)({
    display: 'flex',
    alignItems: 'center',
});

export const LabelIconBlock = styled(Box)({
    padding: 0,
});